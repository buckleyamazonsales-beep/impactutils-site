name: Deploy to Namecheap

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Deploy via SFTP with lftp
        env:
          SFTP_HOST: ${{ secrets.SFTP_HOST }}
          SFTP_USERNAME: ${{ secrets.SFTP_USERNAME }}
          SFTP_PASSWORD: ${{ secrets.SFTP_PASSWORD }}
          SFTP_PORT: ${{ secrets.SFTP_PORT }}
          SFTP_REMOTE_PATH: ${{ secrets.SFTP_REMOTE_PATH }}
        run: |
          lftp -u "$SFTP_USERNAME","$SFTP_PASSWORD" "sftp://$SFTP_HOST:$SFTP_PORT" <<EOF
          set sftp:auto-confirm yes
          set ssl:verify-certificate no
          cd "$SFTP_REMOTE_PATH"
          mkdir -p "$SFTP_REMOTE_PATH/api"
          mkdir -p "$SFTP_REMOTE_PATH/api/data"
          put -O "$SFTP_REMOTE_PATH" index.html
          put -O "$SFTP_REMOTE_PATH" styles.css
          put -O "$SFTP_REMOTE_PATH" app.js
          put -O "$SFTP_REMOTE_PATH" version.json
          put -O "$SFTP_REMOTE_PATH" logo.png
          put -O "$SFTP_REMOTE_PATH/api" api/.htaccess
          put -O "$SFTP_REMOTE_PATH/api" api/admin-lemon-status.php
          put -O "$SFTP_REMOTE_PATH/api" api/admin-site-users.php
          put -O "$SFTP_REMOTE_PATH/api" api/admin-stripe-status.php
          put -O "$SFTP_REMOTE_PATH/api" api/billing-status.php
          put -O "$SFTP_REMOTE_PATH/api" api/create-checkout-session.php
          put -O "$SFTP_REMOTE_PATH/api" api/create-portal-session.php
          put -O "$SFTP_REMOTE_PATH/api" api/forgot-password.php
          put -O "$SFTP_REMOTE_PATH/api" api/hiscores.php
          put -O "$SFTP_REMOTE_PATH/api" api/lemon-env.php
          put -O "$SFTP_REMOTE_PATH/api" api/lemon-secrets.example.php
          put -O "$SFTP_REMOTE_PATH/api" api/lemonsqueezy-webhook.php
          put -O "$SFTP_REMOTE_PATH/api" api/recover-billing.php
          put -O "$SFTP_REMOTE_PATH/api" api/send-verification-email.php
          put -O "$SFTP_REMOTE_PATH/api" api/site-user-register.php
          put -O "$SFTP_REMOTE_PATH/api" api/stripe-billing.php
          put -O "$SFTP_REMOTE_PATH/api" api/stripe-secrets.example.php
          put -O "$SFTP_REMOTE_PATH/api" api/stripe-secrets.php
          put -O "$SFTP_REMOTE_PATH/api" api/stripe-webhook.php
          put -O "$SFTP_REMOTE_PATH/api" api/verify-checkout-session.php
          put -O "$SFTP_REMOTE_PATH/api/data" api/data/.gitkeep
          bye
          EOF
